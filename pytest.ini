[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
# MUDANÇA CRUCIAL: 'session' mantém o loop vivo do início ao fim
asyncio_default_fixture_loop_scope = session
addopts = --cov=src/domain/ledger --cov-report=term-missing --cov-fail-under=100
